# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (only needed if we don't want to commit vendor)
# vendor/

# Go workspace file
go.work
go.work.sum

# Environment variables
.env
.env.*
.env.local
.env.development
.env.staging
.env.production
